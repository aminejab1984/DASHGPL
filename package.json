{
  "name": "dashgpl",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('DASHGPL: static Netlify site — no build required')\""
  },
  "dependencies": {
    "@netlify/blobs": "^11.1.0"
  }
}
